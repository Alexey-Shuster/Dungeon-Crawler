В папке следуюет расположить файлы реализующие класс Game со связкой игрков (PlayerId) с dungeon и управляемой сущностью.
Тут же можно расположить GameManager

На подумать:
GameManager получает StartGameEvent, лезет в регистр лобби, проверяет лидерство, в случае успеха создает Game,
в котором создает dungeon, все сущности, связывает сущности с PlayerId.

В GameManager надо сделать связку PlayerId с Game